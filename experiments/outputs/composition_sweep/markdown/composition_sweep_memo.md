# Behavioral-composition sweep

The stored sweep varies the number of endpoint-policy agents from zero to five in two five-agent populations: cooperative to hoarding and need-based to competitive. Each composition-institution condition uses 100 matched seeds.

The seed-level data are in `csv/summary_by_seed.csv`. Paper-level endpoint contrasts and confidence intervals are generated by `experiments/analysis/analyze_paper_results.py`.
