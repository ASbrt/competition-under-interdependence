# Visualization

Run from the repository root:

```bash
python3 experiments/visualization/make_report_figures.py
```

The script reads the stored experiment and paper-result tables and writes the
four main figures to:

```text
experiments/outputs/report_figures/
```

It does not rerun simulations or Q-learning training.
